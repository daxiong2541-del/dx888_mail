JWT_SECRET=change-me-to-long-random-string

# Neon / Postgres (use one of these)
DATABASE_URL=
NEON_DATABASE_URL=
POSTGRES_URL=

# Bootstrap admin (first login will create the admin user if not exists)
ADMIN_BOOTSTRAP_EMAIL=admin@example.com
ADMIN_BOOTSTRAP_PASSWORD=change-me

# Mail API for public share link fetching
MAIL_API_TOKEN=
MAIL_API_BASE_URL=https://mail.dynmsl.com/api/public

